# Filled by `make fork-sync` from out/_mine/ (generated by `rdfc2im project`).
# Derived, like everything else outside out/*/mapping_*.tsv - not tracked.
# Same convention as bio/sources/intermine-items-large-xml-file/resources/.
*
!.gitignore
