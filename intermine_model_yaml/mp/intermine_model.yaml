# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'mp', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/mp/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Class[MammalianPhenotypeTerm] obo:MP_0000026:
  - a: owl:Class
  - oboinowl:id:
    - MammalianPhenotypeTerm.identifier: MP:0000026  # rdf-config: id (sure)
  - rdfs:label:
    - MammalianPhenotypeTerm.name: "abnormal inner ear morphology"  # rdf-config: label (sure)
  - metadata:treeView*:
    - tree_view: "Class"  # UNMAPPED (drop) - BioPortal browsing artefact; is_a already gives OntologyTerm.parents
  - obo:IAO_0000115:
    - MammalianPhenotypeTerm.description: "any structural anomaly of any components of the labyrinth, including the semicircular canals, vestibule and cochlea"  # rdf-config: definition (sure)
  - oboinowl:hasDbXref*:
    - OntologyTerm.identifier: GO:0042472  # rdf-config: database_cross_reference (sure) -> via MammalianPhenotypeTerm.crossReferences
  - oboinowl:hasExactSynonym*:
    - OntologyTermSynonym.name: "inner ear dysplasia"  # rdf-config: has_exact_synonym (sure) -> via MammalianPhenotypeTerm.synonyms
  - oboinowl:hasOBONamespace:
    - MammalianPhenotypeTerm.namespace: "MPheno.ontology"  # rdf-config: has_obo_namespace (sure)
  - rdfs:subClassOf*:
    - OntologyTerm.identifier: "Class"  # rdf-config: subclass_of (guess) -> via MammalianPhenotypeTerm.parents
  - skos:notation:
    - notation: MP:0000026  # UNMAPPED (drop) - duplicates oboinowl:id
  - obo:IAO_0000231*:
    - has_obsolescence_reason: obo:IAO_0000227  # UNMAPPED (drop) - obsolescence reason
  - obo:IAO_0100001*:
    - term_replaced_by: "Class"  # UNMAPPED (drop) - term_replaced_by: OboParser never reads `replaced_by`; obsoleteness itself is al
  - dc:contributor*:
    - contributor: <https://orcid.org/0000-0002-6490-7723>  # UNMAPPED (drop) - contributor ORCIDs; OboParser never reads them; no OntologyTerm field
  - dc:creator*:
    - creator: <https://orcid.org/0000-0002-6490-7723>  # UNMAPPED (drop) - creator ORCID; OboParser never reads it
  - dc:date*:
    - date: "2017-05-22 15:58:02+00:00"  # UNMAPPED (drop) - record date; OboParser never reads it
  - dcterms:contributor*:
    - terms_contributor: <https://orcid.org/0000-0003-3691-0324>  # UNMAPPED (drop) - contributor ORCIDs; OboParser never reads them; no OntologyTerm field
  - oboinowl:consider*:
    - consider: MP:0001312  # UNMAPPED (drop)
  - oboinowl:created_by*:
    - created_by: "csmith"  # UNMAPPED (drop) - OboParser never reads `created_by`; no OntologyTerm field
  - oboinowl:creation_date?:
    - creation_date: "2010-07-05T10:00:46Z"  # UNMAPPED (drop) - OboParser never reads `creation_date`; no OntologyTerm field
  - oboinowl:hasAlternativeId*:
    - OntologyTermSynonym.name: MP:0000014  # rdf-config: has_alternative_id (sure) -> via MammalianPhenotypeTerm.synonyms
  - oboinowl:hasBroadSynonym*:
    - OntologyTermSynonym.name: "auditory abnormalities"  # rdf-config: has_broad_synonym (sure) -> via MammalianPhenotypeTerm.synonyms
  - oboinowl:hasNarrowSynonym*:
    - OntologyTermSynonym.name: "abnormal ear development/ ear malformation"  # rdf-config: has_narrow_synonym (sure) -> via MammalianPhenotypeTerm.synonyms
  - oboinowl:hasRelatedSynonym*:
    - OntologyTermSynonym.name: "abnormal eardrum morphology"  # rdf-config: has_related_synonym (sure) -> via MammalianPhenotypeTerm.synonyms
  - oboinowl:inSubset*:
    - in_subset: obo:mp#CvDC_Terms  # UNMAPPED (todo) - spec D2 - still open, but narrower than it looks: OboParser never reads `subset`
  - rdfs:comment?:
    - comment: "Heart rate variability (HRV) is the variation of beat-to-beat intervals. ..."  # UNMAPPED (drop) - OboParser never reads `comment`, and OntologyTerm has no field for it (descripti
  - owl:deprecated?:
    - MammalianPhenotypeTerm.obsolete: "1"  # rdf-config: deprecated (guess)
