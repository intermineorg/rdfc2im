# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'go', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/go/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Class[GOTerm] obo:GO_0044255:
  - a: rdfs:Resource
  - oboinowl:id+:
    - GOTerm.identifier: GO:0044255  # rdf-config: id (sure)
  - rdfs:label+:
    - GOTerm.name: "cellular lipid metabolic process"  # rdf-config: label (sure)
  - obo:IAO_0000115+:
    - GOTerm.description: "The chemical reactions and pathways involving lipids, as carried out by individual cells."  # rdf-config: definition (sure)
  - oboinowl:hasDbXref+:
    - OntologyTerm.identifier: Reactome:REACT_28392  # rdf-config: xref (sure) -> via GOTerm.crossReferences
  - oboinowl:hasExactSynonym+:
    - OntologyTermSynonym.name: "cellular lipid metabolism"  # rdf-config: synonym (sure) -> via GOTerm.synonyms
  - oboinowl:hasOBONamespace+:
    - GOTerm.namespace: "biological_process"  # rdf-config: namespace (sure)
  - oboinowl:inSubset*:
    - subset: go:goslim_pir  # UNMAPPED (todo) - spec D2 - still open, but narrower than it looks: OboParser never reads `subset`
  - rdfs:subClassOf+:
    - OntologyTerm.identifier: obo:GO_0044237  # rdf-config: superclass (guess) -> via GOTerm.parents
