# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'clinvar', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/clinvar/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Clinvar[Allele] clinvar:209597:
  - a: cvo:VariationArchiveType
  - cvo:accession:
    - Allele.secondaryIdentifier: "VCV000209597"  # rdf-config: vcv (sure)
  - cvo:classified_record?:
    - _bn1[Allele]:
      - a: cvo:ClassifiedRecord
      - cvo:rcv_list:
        - _bn2[Allele]:
          - a: cvo:RCVList
          - cvo:rcv_accession+:
            - _bn3[Allele]:
              - a: cvo:TypeRCV
              - cvo:accession:
                - rcv_accession: "RCV000191543"  # UNMAPPED (drop) - RCV accession; the converter never loads RCV records
              - cvo:classified_condition_list:
                - _bn4[Allele]:
                  - a: cvo:ClassifiedConditionList
                  - cvo:classified_condition+:
                    - _bn5[Disease]:
                      - a: med2rdf:Disease
                      - dct:identifier?:
                        - disease_id: "C2675520"  # UNMAPPED (todo) - spec D5 - ClinVar gives MedGen; the converter parses OMIM out of line[12] for Di
                      - dct:references*:
                        - disease_reference: medgen:C2675520  # UNMAPPED (todo) - spec D5 - ClinVar gives MedGen; the converter parses OMIM out of line[12] for Di
                      - dct:source?:
                        - disease_source: "MedGen"  # UNMAPPED (todo) - spec D5 - ClinVar gives MedGen; the converter parses OMIM out of line[12] for Di
                      - rdfs:label?:
                        - Disease.name: "Breast-ovarian cancer, familial, susceptibility to, 2"  # rdf-config: disease_label (sure)
                  - cvo:trait_set_id:
                    - trait_set_id: "2560"  # UNMAPPED (drop) - trait set id; the converter never writes it and Allele has no field
              - cvo:rcv_classifications:
                - _bn6[Allele]:
                  - a: cvo:RCVClassifications
                  - cvo:germline_classification?:
                    - _bn7[Allele]:
                      - a: cvo:GermlineClassification
                      - cvo:description?:
                        - _bn8[Allele]:
                          - a: cvo:Description
                          - cvo:date_last_evaluated?:
                            - rcv_date_last_evaluated: "2015-01-12"  # UNMAPPED (drop) - RCV evaluation date; the converter never writes it and Allele has no field
                          - cvo:description:
                            - Allele.clinicalSignificance: "Benign"  # rdf-config: interpretation (sure)
                          - cvo:submission_count:
                            - Allele.submissionCount: "1"  # rdf-config: rcv_submission_count (guess)
                      - cvo:review_status?:
                        - Allele.reviewStatus: "reviewed by expert panel"  # rdf-config: rcv_review_status (guess)
              - cvo:title:
                - rcv_title: "NM_000059.3(BRCA2):c.-764A>G AND Breast-ovarian cancer, familial, susceptibility to, 2"  # UNMAPPED (drop) - RCV title; the converter never loads RCV records
              - cvo:version:
                - rcv_version: "10"  # UNMAPPED (drop) - RCV version; the converter never loads RCV records
              - dct:identifier:
                - rcv_id: "RCV000191543"  # UNMAPPED (drop) - RCV id; the converter never loads RCV records
      - sio:SIO_000628*:
        - _bn9[Allele]:
          - a: cvo:TypeAllele
          - faldo:location*:
            - _bn10[Allele]:
              - a: faldo:ExactPosition
              - faldo:position?:
                - position: "32889080"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - faldo:reference*:
                - reference: hco:13\#GRCh37  # UNMAPPED (drop) - reference sequence IRI, not the allele base; name-match false positive. Allele.r
              - cvo:accession?:
                - refseq: "NC_000013.10"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - cvo:assembly?:
                - assembly: "GRCh37"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - cvo:assembly_accession_version?:
                - assembly_accession_version: "GCF_000001405.25"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - cvo:assembly_status?:
                - assembly_status: "previous"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - cvo:chr?:
                - chromosome: "13"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - cvo:variant_length?:
                - variant_length: "1"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
              - rdfs:label?:
                - exact_position_bnode_label: "13:32889080:GRCh37"  # UNMAPPED (drop) - the label of the variant LOCATION blank node (e.g. 12:51864290:GRCh37), flattene
          - med2rdf:alternative_allele*:
            - Allele.alternate: "G"  # rdf-config: alternative_allele (sure)
          - med2rdf:gene*:
            - ncbigene: "Ncbigene"  # UNMAPPED (link)
          - med2rdf:reference_allele*:
            - Allele.reference: "A"  # rdf-config: reference_allele (sure)
          - cvo:allele_frequency*:
            - _bn11[Allele]:
              - a: cvo:AlleleFrequency
          - cvo:allele_id?:
            - Allele.primaryIdentifier: "205807"  # rdf-config: allele_id (sure)
          - cvo:canonical_spdi?:
            - spdi: NC_000013.11:32314942:A:G  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
          - cvo:cytogenetic_location*:
            - variant_location: "13q13.1"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
          - cvo:global_minor_allele_frequency?:
            - _bn12[Allele]:
              - a: cvo:GlobalMinorAlleleFrequency
              - cvo:minor_allele?:
                - minor_allele: "G"  # UNMAPPED (drop) - minor allele; the converter never loads allele frequency
              - cvo:source:
                - frequency_source: "1000 Genomes Project"  # UNMAPPED (drop) - MAF source; the converter never loads allele frequency
              - cvo:value:
                - frequency: "+0.04014"  # UNMAPPED (drop) - MAF value; the converter never loads allele frequency
  - cvo:date_created:
    - date_created: "2015-09-29"  # UNMAPPED (drop) - record creation date; the converter never writes it and Allele has no field
  - cvo:date_last_updated:
    - date_last_updated: "2024-11-03"  # UNMAPPED (drop) - record update date; the converter never writes it and Allele has no field
  - cvo:most_recent_submission?:
    - most_recent_submission: "2015-09-29"  # UNMAPPED (drop) - submission date; the converter never writes it and Allele has no field
  - cvo:number_of_submissions:
    - number_of_submissions: "1"  # UNMAPPED (drop) - duplicates cvo:submission_count, already mapped to Allele.submissionCount
  - cvo:number_of_submitters:
    - number_of_submitters: "1"  # UNMAPPED (drop) - submitter count; the converter never writes it and Allele has no field
  - cvo:record_status:
    - recode_status: "current"  # UNMAPPED (drop) - record status; the converter never writes it and Allele has no field
  - cvo:record_type:
    - record_type: "classified"  # UNMAPPED (drop) - record type; the converter never writes it and Allele has no field
  - cvo:species:
    - species: "Homo sapiens"  # UNMAPPED (drop) - always Homo sapiens; add a const Organism.taxonId=9606 column instead
  - cvo:variation_id:
    - id: "209597"  # UNMAPPED (todo) - ClinVar VariationID, the number inside the VCV accession; redundant with it unle
  - cvo:variation_name:
    - name: "NM_000059.3(BRCA2):c.-764A>G"  # UNMAPPED (drop) - Identical to rdfs:label in every fetched row, and both wrote Allele.name on the 
  - cvo:variation_type:
    - Allele.type: "single nucleotide variant"  # rdf-config: type (sure)
  - cvo:version:
    - version: "3"  # UNMAPPED (drop) - record version; the converter never writes it and Allele has no field
  - dct:created:
    - create_date: "2015-09-29"  # UNMAPPED (drop)
  - dct:modified:
    - modified_date: "2024-11-03"  # UNMAPPED (drop)
  - rdfs:label:
    - Allele.name: "NM_000059.3(BRCA2):c.-764A>G"  # rdf-config: label (sure)

- Ncbigene[Gene] ncbigene:106721785:
  - a: cvo:Gene
  - faldo:location*:
    - _bn13[Gene]:
      - a: faldo:Region
      - faldo:begin*:
        - _bn14[Gene]:
          - a: faldo:ExactPosition
          - faldo:position?:
            - gene_begin: "32314559"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - faldo:end*:
        - _bn15[Gene]:
          - a: faldo:ExactPosition
          - faldo:position?:
            - gene_end: "32315789"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:accession*:
        - chromosome_accession: "NC_000013.11"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:assembly:
        - gene_assembly: "GRCh38"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:assembly_accession_version?:
        - gene_assembly_accession: "GCF_000001405.38"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:assembly_status?:
        - gene_assembly_status: "current"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:chr:
        - gene_chromosome: "13"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
      - cvo:strand?:
        - gene_strand: "+"  # UNMAPPED (todo) - spec D13 - the stock converter loads no coordinates, so dropping matches HumanMi
  - cvo:cytogenetic_location*:
    - gene_location: "13q13.1"  # UNMAPPED (drop) - gene cytoLocation; the converter writes only Gene.primaryIdentifier, ncbi-gene/h
  - cvo:full_name*:
    - gene_name: "BRCA2 promoter/silencer region"  # UNMAPPED (drop) - gene name; the converter writes only Gene.primaryIdentifier, ncbi-gene/hgnc own 
  - cvo:gene_id?:
    - Gene.primaryIdentifier: "106721785"  # rdf-config: ncbigene_id (sure)
  - cvo:relationship_type*:
    - relationship_type: "genes overlapped by variant"  # UNMAPPED (drop) - variant/gene relationship type; the converter never writes it and Allele has no 
  - cvo:source*:
    - source: "calculated"  # UNMAPPED (drop) - gene-relationship source; the converter never writes it and Allele has no field
  - cvo:symbol*:
    - Gene.symbol: "LOC106721785"  # rdf-config: symbol (sure)
  - rdfs:seeAlso:
    - hgnc: hgnc:20444  # UNMAPPED (drop) - HGNC xref; the converter writes only Gene.primaryIdentifier, ncbi-gene/hgnc own
