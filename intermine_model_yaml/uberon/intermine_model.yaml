# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'uberon', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/uberon/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Class[AnatomyTerm] obo:UBERON_0002048:
  - a: owl:Class
  - oboinowl:id:
    - AnatomyTerm.identifier: UBERON:00002048  # rdf-config: id (sure)
  - rdfs:label:
    - AnatomyTerm.name: "lung"  # rdf-config: label (sure)
  - obo:IAO_0000115:
    - AnatomyTerm.description: "Respiration organ that develops as an outpocketing of the esophagus."  # rdf-config: definition (sure)
  - obo:IAO_0000116*:
    - editor_note: "TODO - check frog/fish"  # UNMAPPED (drop) - editor_note; OboParser never reads it
  - obo:IAO_0000232*:
    - curator_note: "See notes for pars intermedia of adenohypophysis."  # UNMAPPED (drop) - curator_note; OboParser never reads it
  - obo:IAO_0006012*:
    - scheduled_for_obsoletion_on_or_after: "2023-04-20"  # UNMAPPED (drop) - scheduled_for_obsoletion_on_or_after; OboParser never reads it
  - obo:IAO_0100001*:
    - term_replaced_by: UBERON:0002275  # UNMAPPED (drop) - term_replaced_by: OboParser never reads `replaced_by`; obsoleteness itself is al
  - obo:RO_0002161*:
    - never_in_taxon: obo:NCBITaxon_32443  # UNMAPPED (drop) - never_in_taxon - taxon constraint; OboParser never reads it
  - obo:RO_0002171*:
    - mutually_spatially_disjoint_with: "Class"  # UNMAPPED (drop) - mutually_spatially_disjoint_with - OWL axiom; OboParser never reads it
  - obo:RO_0002173*:
    - ambiguous_for_taxon: obo:NCBITaxon_8782  # UNMAPPED (drop) - ambiguous_for_taxon - taxon constraint; OboParser never reads it
  - obo:RO_0002174*:
    - dubious_for_taxon: obo:NCBITaxon_10090  # UNMAPPED (drop) - dubious_for_taxon - taxon constraint; OboParser never reads it
  - obo:RO_0002175*:
    - present_in_taxon: obo:NCBITaxon_9606  # UNMAPPED (drop) - present_in_taxon - taxon constraint; OboParser never reads it
  - obo:RO_0002475*:
    - has_no_connections_with: "Class"  # UNMAPPED (drop) - has_no_connections_with - OWL axiom; OboParser never reads it
  - obo:UBPROP_0000001*:
    - external_definition: "Dense regular connective tissue that connects muscle to bone.[VSAO]"  # UNMAPPED (drop) - external_definition (the definition proper is already IAO_0000115 -> description
  - obo:UBPROP_0000002*:
    - axiom_lost_from_external_ontology: "relationship loss: overlaps hyomandibular-otic region joint (TAO:0001947)[TAO]"  # UNMAPPED (drop) - axiom_lost_from_external_ontology; OboParser never reads it
  - obo:UBPROP_0000003*:
    - homology_notes: "(...) an essentially similar sequence of events occurs during the embryonic development of the vertebrate eye. The eye initially develops as a single median evagination of the diencephalon that soon bifurcates to form the paired optic vesicles. As each optic vesicle grows towards the body surface, its proximal part narrows as the optic stalk, and its distal part invaginates to form a two-layered optic cup. (...) The outer layer of the optic cup becomes the pigment layer of the retina, whereas the inner layer differentiates into the photoreceptive cells and neuronal layers of the retina.[well established][VHOG]"  # UNMAPPED (drop) - homology_notes; OboParser never reads it
  - obo:UBPROP_0000004*:
    - provenance_notes: "This class was sourced from an external ontology (teleost_anatomy). Its definitions, naming conventions and relationships may need to be checked for compatibility with uberon"  # UNMAPPED (drop) - provenance_notes; OboParser never reads it
  - obo:UBPROP_0000005*:
    - external_comment: "[In TAO], This term was made obsolete because it is considered a synonym of membrane bone (VSAO:0000023).[TAO]"  # UNMAPPED (drop) - external_comment; OboParser never reads it
  - obo:UBPROP_0000006*:
    - implements_design_pattern: <https://github.com/obophenotype/uberon/wiki/Modeling-paired-structures-Design-Pattern>  # UNMAPPED (drop) - implements_design_pattern; OboParser never reads it
  - obo:UBPROP_0000007*:
    - has_relational_adjective: "ventral"  # UNMAPPED (drop) - has_relational_adjective; OboParser never reads it
  - obo:UBPROP_0000008*:
    - taxon_notes: "the basic trilaminar structure of the dentate gyrus is common across all species studied, with relatively minimal phylogenetic variation[PMC2492885]"  # UNMAPPED (drop) - taxon_notes; OboParser never reads it
  - obo:UBPROP_0000009*:
    - function_notes: "vomiting center in humans"  # UNMAPPED (drop) - function_notes; OboParser never reads it
  - obo:UBPROP_0000010*:
    - structure_notes: "A secretion of the prostate this is slightly alkaline fluid, milky or white in appearance. Usually constitutes 20-30% of the volume of the semen along with spermatozoa and seminal vesicle fluid [Wikipedia:Prostate#Secretions]"  # UNMAPPED (drop) - structure_notes; OboParser never reads it
  - obo:UBPROP_0000011*:
    - development_notes: "contributes significantly in building up the GABAergic cortical cell population[WP]"  # UNMAPPED (drop) - development_notes; OboParser never reads it
  - obo:UBPROP_0000012*:
    - external_ontology_notes: "this is a 'set of anatomical planes' in FMA"  # UNMAPPED (drop) - external_ontology_notes; OboParser never reads it
  - obo:UBPROP_0000013*:
    - terminology_notes: "Gilbert uses visceral endoderm as a synonym for primitive endoderm/hypoblast"  # UNMAPPED (drop) - terminology_notes; OboParser never reads it
  - obo:UBPROP_0000014*:
    - actions_notes: "In some mammals these muscles can adjust the direction of the pinna. In humans these muscles possess very little action. The auricularis anterior draws the auricula forward and upward; the Auricularis superior slightly raises it; and the Auricularis posterior draws it backward."  # UNMAPPED (drop) - actions_notes; OboParser never reads it
  - obo:UBPROP_0000015*:
    - location_notes: "Bounded medially by the Lamina orbitalis of the Os ethmoidale and laterally by the Maxilla and Os palatinum and in the dog also by the Os lacrimale[NOMINA ANATOMICA VETERINARIA (2005)]"  # UNMAPPED (drop) - location_notes; OboParser never reads it
  - obo:UBPROP_0000103*:
    - pharyngeal_arch_number: "7"  # UNMAPPED (drop) - pharyngeal_arch_number; OboParser never reads it
  - obo:UBPROP_0000104*:
    - ray_number: "6"  # UNMAPPED (drop) - ray_number; OboParser never reads it
  - obo:UBPROP_0000105*:
    - phalanx_number: "1"  # UNMAPPED (drop) - phalanx_number; OboParser never reads it
  - obo:UBPROP_0000106*:
    - rib_number: "12"  # UNMAPPED (drop) - rib_number; OboParser never reads it
  - obo:UBPROP_0000107*:
    - vertebra_number: "1"  # UNMAPPED (drop) - vertebra_number; OboParser never reads it
  - obo:UBPROP_0000108*:
    - somite_number: "26"  # UNMAPPED (drop) - somite_number; OboParser never reads it
  - obo:UBPROP_0000111*:
    - rhombomere_number: "1"  # UNMAPPED (drop) - rhombomere_number; OboParser never reads it
  - obo:UBPROP_0000112*:
    - tooth_number: "1"  # UNMAPPED (drop) - tooth_number; OboParser never reads it
  - obo:UBPROP_0000201*:
    - source_atlas: obo:Harvard-Oxford_Atlas  # UNMAPPED (drop) - source_atlas; OboParser never reads it
  - obo:UBPROP_0000202*:
    - fma_set_term: obo:FMA_71364  # UNMAPPED (drop) - fma_set_term; OboParser never reads it
  - dc:contributor*:
    - dc_contributor: <https://github.com/cerivs>  # UNMAPPED (drop) - contributor ORCIDs; OboParser never reads them; no OntologyTerm field
  - dcterms:contributor*:
    - dcterms_contributor: <https://orcid.org/0000-0001-9114-8737>  # UNMAPPED (drop) - contributor ORCIDs; OboParser never reads them; no OntologyTerm field
  - dcterms:date*:
    - date: "2021-06-02 00:19:07+00:00"  # UNMAPPED (drop) - record date; OboParser never reads it
  - oboinowl:consider*:
    - consider: BIRNLEX:1003  # UNMAPPED (drop)
  - oboinowl:created_by*:
    - created_by: <http://orcid.org/0000-0002-2825-0621>  # UNMAPPED (drop) - OboParser never reads `created_by`; no OntologyTerm field
  - oboinowl:creation_date*:
    - creation_date: "2009-07-30 07:57:38+00:00"  # UNMAPPED (drop) - OboParser never reads `creation_date`; no OntologyTerm field
  - oboinowl:hasAlternativeId*:
    - OntologyTermSynonym.name: UBERON:0003263  # rdf-config: has_alternative_id (sure) -> via AnatomyTerm.synonyms
  - oboinowl:hasBroadSynonym*:
    - OntologyTermSynonym.name: "reticular formation"  # rdf-config: has_broad_synonym (sure) -> via AnatomyTerm.synonyms
  - oboinowl:hasDbXref*:
    - OntologyTerm.identifier: CALOHA:TS-1266  # rdf-config: database_cross_reference (sure) -> via AnatomyTerm.crossReferences
  - oboinowl:hasExactSynonym*:
    - OntologyTermSynonym.name: "NA"  # rdf-config: has_exact_synonym (sure) -> via AnatomyTerm.synonyms
  - oboinowl:hasNarrowSynonym*:
    - OntologyTermSynonym.name: "elastic fiber"  # rdf-config: has_narrow_synonym (sure) -> via AnatomyTerm.synonyms
  - oboinowl:hasOBONamespace*:
    - AnatomyTerm.namespace: "uberon"  # rdf-config: has_obo_namespace (sure)
  - oboinowl:hasRelatedSynonym*:
    - OntologyTermSynonym.name: "basal ganglia"  # rdf-config: has_related_synonym (sure) -> via AnatomyTerm.synonyms
  - oboinowl:inSubset*:
    - in_subset: obo:uberon/core#early_development  # UNMAPPED (todo) - spec D2 - still open, but narrower than it looks: OboParser never reads `subset`
  - rdfs:comment*:
    - comment: "Taxon notes:" the ascidian tube-like heart lacks chambers....The ascidian heart is formed after metamorphosis as a simple tube-like structure with a single-layered myoepithelium that is continuous with a single-layered pericar- dial wall. It lacks chambers and endocardium.... The innovation of the chambered heart was a key event in vertebrate evolution, because the chambered heart generates one-way blood flow with high pressure, a critical requirement for the efficient blood supply of large-body vertebrates... all extant vertebrates have hearts with two or more chambers (Moorman and Christoffels 2003)" DOI:10.1101/gad.1485706"  # UNMAPPED (drop) - OboParser never reads `comment`, and OntologyTerm has no field for it (descripti
  - rdfs:seeAlso*:
    - see_also: <https://github.com/obophenotype/uberon/issues/336>  # UNMAPPED (drop)
  - rdfs:subClassOf*:
    - OntologyTerm.identifier: "Class"  # rdf-config: subclass_of (guess) -> via AnatomyTerm.parents
  - owl:deprecated*:
    - AnatomyTerm.obsolete: "1"  # rdf-config: deprecated (guess)
  - owl:disjointWith*:
    - disjoint_with: "Class"  # UNMAPPED (drop) - OWL disjointness axiom; OboParser never reads it
  - owl:equivalentClass*:
    - equivalent_class: "Class"  # UNMAPPED (drop) - OWL equivalence axiom; OboParser never reads it
  - foaf:depicted_by*:
    - depicted_by: <http://upload.wikimedia.org/wikipedia/commons/7/77/Gray709.png>  # UNMAPPED (drop) - image URL; OboParser never reads it
