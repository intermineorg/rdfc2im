# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'ncbigene', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/ncbigene/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Gene[Gene] ncbigene:34:
  - a: insdc:Gene
  - dct:identifier:
    - Gene.primaryIdentifier: "34"  # rdf-config: id (sure)
  - rdfs:label:
    - Gene.symbol: "ACADM"  # rdf-config: label (sure)
  - insdc:standard_name?:
    - standard_name: "ACADM"  # UNMAPPED (drop) - duplicates rdfs:label
  - insdc:locus_tag?:
    - locus_tag: ""  # UNMAPPED (drop) - empty for human genes; the converter does not read it
  - insdc:gene_synonym*:
    - Synonym.value: "ACAD1"  # rdf-config: gene_synonym (sure) -> via Gene.synonyms
  - dct:description?:
    - Gene.description: "acyl-CoA dehydrogenase medium chain"  # rdf-config: gene_description (sure)
  - dct:alternative*:
    - Synonym.value: "medium-chain specific acyl-CoA dehydrogenase, mitochondrial"  # rdf-config: alternative (sure) -> via Gene.synonyms
  - insdc:dblink*:
    - Gene.secondaryIdentifier: hgnc:89  # rdf-config: dblink (sure)
  - insdc:db_xref*:
    - CrossReference.identifier: AllianceGenome:HGNC:89  # rdf-config: db_xref (sure) -> via Gene.crossReferences
  - ncbio:typeOfGene:
    - Gene.typeOfGene: "protein-coding"  # rdf-config: type_of_gene (guess)
  - ncbio:nomenclatureStatus?:
    - nomenclature_status: "official"  # UNMAPPED (drop) - the converter does not read it; no Gene field
  - ncbio:fullName?:
    - Gene.name: "acyl-CoA dehydrogenase medium chain"  # rdf-config: full_name (sure)
  - ncbio:featureType*:
    - feature_type: ""  # UNMAPPED (drop) - empty for human genes; the converter sets no sequenceOntologyTerm, and typeOfGen
  - ncbio:taxid:
    - Organism.taxonId: taxid:9606  # rdf-config: taxid (sure) -> via Gene.organism
  - insdc:chromosome?:
    - Chromosome.primaryIdentifier: "1"  # rdf-config: chromosome (guess) -> via Gene.chromosome
  - insdc:map?:
    - Gene.cytoLocation: "1p31.1"  # rdf-config: map (sure)
  - dct:modified:
    - modified: "2021-02-07"  # UNMAPPED (todo) - spec D1 - narrower than it looks: NcbiGeneConverter never stored a modification
