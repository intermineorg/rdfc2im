# Generated by tools/gen_intermine_model_yaml.py - DO NOT EDIT, re-run after `make translate`.
# Same list/predicate/cardinality syntax as rdf-config's own model.yaml for 'homologene', with
# each local variable name relabelled to the InterMine Class.field it maps onto, where
# out/homologene/mapping_predicates.sssom.tsv records a real (sure/guess/human) mapping. Anything
# todo/drop/link, or with no matching mapping row, keeps its original rdf-config label and
# is marked UNMAPPED in an inline comment - nothing is silently dropped or guessed at.

- Group[Homologue] homologene:3:
  - a: orth:OrthologsCluster
  - dct:identifier:
    - group_id: "3"  # UNMAPPED (todo) - spec D7 - cluster metadata; meaningful only once clusters expand to pairwise Hom
  - rdfs:label:
    - group_label: "Gene conserved in Bilateria"  # UNMAPPED (todo) - spec D7 - cluster metadata; meaningful only once clusters expand to pairwise Hom
  - orth:inDataset:
    - dataset: "Dataset"  # UNMAPPED (link)
  - orth:hasHomologousMember+:
    - gene: "Gene"  # UNMAPPED (todo) - cluster members are Gene IRIs (ncbigene:NNN); Homologue is pairwise, so the clus

- Dataset[?] <https://ncbi.nlm.nih.gov/homologene/>:
  - a: orth:OrthologyDataset
  - dct:title:
    - dataset_title: "HomoloGene Release 68"  # UNMAPPED (todo) - subject not bound to a class (see subjects.tsv)

- Gene[Gene] ncbigene:34:
  - a: orth:Gene
  - rdfs:label:
    - Gene.symbol: "ACADM"  # rdf-config: gene_label (sure)
  - obo:RO_0002162:
    - Organism.taxonId: "Taxon"  # rdf-config: taxon (sure) -> via Gene.organism
  - sio:SIO_010078:
    - Protein.primaryIdentifier: "Protein"  # rdf-config: ncbiprotein (sure) -> via Gene.proteins

- Taxon[Organism] taxonomy:9606:
  - a: taxon:Taxon

- Protein[Protein] ncbiprotein:NP_000007.1:
  - a: orth:Protein
